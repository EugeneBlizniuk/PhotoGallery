import React from "react";

const GaleryView = () => {
		return (
				<div className="no-albums-found">
					<span>Create at least one album first!</span>
 				</div>  
		);
};

export default GaleryView;
